feat: congé rapide au clic, RTT comptée comme travail effectif, vue mensuelle et raccourcis enrichis (v2.1.0)

- Ajout de congé en un clic depuis le calendrier : cliquer sur un jour libre
  ouvre une mini-fenêtre (type + note) qui enregistre directement le congé,
  sans passer par le formulaire complet. Nouvelle modale #quickAddCongeModal,
  fonctions closeQuickAddCongeModal()/submitQuickAddConge()/
  openFullCongeFormFromQuickAdd(). openAddCongeForDate() adapté.

- RTT comptée comme temps de travail effectif (option la plus sûre côté
  paie) : le compteur du jour/de la semaine, le solde d'heures sup et la vue
  mensuelle intègrent désormais les jours de RTT posés comme du temps
  travaillé, au lieu de les laisser à 0h. Nouvelles fonctions
  isDateOnRTT(), getGoalForDow(), getRTTGoalMsForRange(). Mise à jour de
  getWeekGoalForMonday(), getMonthGoal(), renderObjectifs(),
  getOvertimeBalance() et de la vue mensuelle de l'historique.

- Vue mensuelle de l'historique enrichie : barre de progression et
  pourcentage d'objectif atteint par mois, badge indiquant la part d'heures
  RTT incluses dans le total.

- Automatisations Raccourcis enrichies : nouvelle section "Arrêt
  automatique à l'arrivée chez soi" (guide automatisation personnelle
  native, ?action=stop, fonctionne app fermée) et nouveau réglage
  "Notifications de confirmation" (shortcutNotifEnabled) affichant une
  Notification native quand une action est déclenchée via URL. Nouvelles
  fonctions getShortcutNotifEnabled()/onShortcutNotifToggle()/
  notifyShortcutActionResult(). handleQuickAction() enrichi (notification +
  gestion reprise après pause).

- Bump version : 2.0.8 → 2.1.0 (APP_VERSION + CACHE_VERSION synchronisés).
- Entrée changelog v2.1.0 ajoutée.

Fichiers modifiés : index.html, sw.js, README.md
